# Private Operator Records

Identity, registration, tax, KYC, legal and full changeover records are stored in
the ignored `private/` area. They are excluded from Git and the dashboard, but
included in encrypted runtime backups so Codex can use them when an operator-only
task genuinely requires them.

Tracked documentation must refer to the completion state only. It must not repeat
personal identifiers, account references or identity evidence.
