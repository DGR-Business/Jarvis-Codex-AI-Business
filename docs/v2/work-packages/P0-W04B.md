# P0-W04B — Exact Renderer Inventory Corrective Package

**Phase:** P0 — V2.1.1 engineering operating system\
**Status:** in progress\
**Preferred agent:** Codex\
**Handoff allowed:** yes, after a truthful checkpoint\
**Owner approval:** recorded by the fresh-session instruction dated 2026-08-16\
**Finding:** `P0-GATE-02-F01` in `PHASE-GATE-REVIEW-02.md`

## 1. Authority and immutable anchors

This package is the only corrective authority created by the second failed
Phase 0 gate. It is governed by the Master Plan, the Phase 0 Execution Pack and
its owner-approved P0-W04B amendment, this focused contract, AGENTS.md and the
active repository workflow skill, in that order.

The starting anchors are exact:

- baseline/local `main` (`B`):
  `718e50670812ad5da7210bd9f183521328cccf93`;
- preserved WIP checkpoint:
  `612a35c8b1d881f638570373d06f99d26bfb280e`;
- reviewed P0-W04A candidate (`S2`):
  `e37c958a232e139f6216caae66b5b7932b6a6e0b`;
- second gate review (`R2`) and required A2 parent:
  `d4819528050b42a2dcefea590c106d912fc5e0cf`;
- original review blob:
  `6928a40ea2c1f0af493e728e6a71084d13291089`; and
- second review blob:
  `d5bf6cddde6093126f7fb008d7f112bc214f7eb5`.

Work is confined to `C:\Pantheon-worktrees\P0-engineering-os` on
`codex/p0-engineering-os`. Both existing review reports are immutable.

## 2. Objective and current finding

Correct only `P0-GATE-02-F01`: the P0-W04A environment validates four direct
roots but does not prove the exact resolver-required transitive and bootstrap
tooling inventory. An unexpected distribution, changed or missing transitive,
duplicate normalized metadata record, or changed pip can therefore escape the
current equality check and may be reused.

The result must be one reproducible, isolated, checkout-local CPython 3.13
renderer whose complete installed distribution inventory is exact and whose
shared resolver continues to serve Doctor, ordinary tests, approval-pack
rendering and product rendering.

This package changes no renderer algorithm, rendered product byte contract,
buyer/commercial behavior, database/runtime schema, UI or provider adapter.

## 3. Exact inventory contract

`requirements-runtime.txt` remains limited to these four direct runtime roots:

```text
openpyxl==3.1.5
Pillow==12.3.0
pypdfium2==5.13.0
reportlab==5.0.0
```

At most one additional small tracked full-inventory constraints contract may
be added, preferably `requirements-renderer-lock.txt`. After distribution-name
normalization it must contain exactly one record for each of:

```text
openpyxl==3.1.5
Pillow==12.3.0
pypdfium2==5.13.0
reportlab==5.0.0
charset-normalizer==3.5.1
et_xmlfile==2.0.0
pip==26.2.1
```

Pip is explicitly governed bootstrap tooling. No hash manifest, dependency
database, assurance framework, generated lock state or second programme state
system may be created.

## 4. Authorized implementation

1. Bootstrap creates an isolated CPython 3.13 `/.venv-renderer/` and normalizes
   the complete seven-distribution inventory.
2. Public PyPI is authorized only for those exact releases, with inherited
   indexes/configuration/credentials suppressed. No other package download is
   authorized.
3. Exact deletion/recreation of the resolved, non-linked checkout-local
   `/.venv-renderer/` is authorized. No other directory may be replaced.
4. Contract parsing normalizes distribution names, rejects duplicate or
   unexpected contract entries, and proves that the four roots agree between
   the root and full-inventory files.
5. Runtime validation rejects every unexpected, missing or duplicate-normalized
   installed distribution and every root, transitive or pip version mismatch.
6. Validation continues to reject foreign interpreters, system-site
   inheritance, user-site access, failed required imports and failed
   `pip check`.
7. Reuse is allowed only when the complete inventory, local interpreter
   provenance and requested base provenance pass. Any drift forces the existing
   anchored safe rebuild path or fails closed.
8. Doctor, ordinary tests, approval-pack rendering and product rendering retain
   the single canonical resolver. The renderer implementations themselves are
   unchanged.
9. Hosted CI treats `actions/setup-python` as the verified CPython 3.13 base,
   bootstraps and checks `/.venv-renderer/`, and runs ordinary tests through
   that canonical local environment.
10. Recovery source validation includes the tracked full-inventory contract;
    recovery/source backups continue to exclude generated `/.venv-renderer/`
    files.

The known diagnostic temporary-path limitation in R2 is explicitly deferred
and must not be absorbed into this work.

## 5. Expected implementation surfaces

The bounded surface is:

- the single new inventory contract;
- `src/runtime/renderer-environment.js` and its existing CLI/policy text;
- `.github/workflows/ci.yml`;
- the existing recovery source contract in `src/runtime/backup.js`;
- Doctor diagnostics only where needed to report the strengthened shared
  contract;
- governance/status state; and
- focused renderer, runtime-dependency, isolation, Doctor, recovery and status
  tests plus only directly affected recovery fixture helpers.

`requirements-runtime.txt`, renderer Python algorithms, product/approval-pack
business code, package-lock, provider/UI/database surfaces and unrelated tests
must remain unchanged unless a required check proves a directly caused fixture
update is necessary and it stays within this contract.

## 6. Required focused proof

Focused coverage must prove:

- exact clean inventory passes;
- an unexpected distribution fails;
- a changed or missing transitive fails;
- duplicate normalized metadata fails;
- governed pip mismatch fails;
- a polluted environment is never reused;
- a fresh bootstrap creates the exact inventory and an immediate second call
  reports `reused=true`;
- coherent local and hosted canonical discovery pass;
- foreign, relative, one-sided or incoherent aliases fail; and
- Doctor, product renderer, approval-pack renderer, recovery-source validation
  and the ordinary wrapper retain their required fail-closed behavior.

## 7. Required final verification

Run only the authorized ladder, in this order:

```powershell
npm.cmd run renderer:bootstrap -- --python <verified-absolute-cpython-3.13> --recreate
npm.cmd run renderer:check
npm.cmd test -- test/renderer-environment.test.js test/runtime-rendering-dependencies.test.js test/test-environment-isolation.test.js test/pantheon-backup-doctor.test.js test/digital-product-file-factory-hardening.test.js test/agent-runtime-renderer-refresh-hardening.test.js test/startup-readiness.test.js test/v2-status.test.js
npm.cmd test -- test/pantheon-backup-recovery-set.test.js
npm.cmd test -- test/pantheon-production.test.js
npm.cmd run lint
Remove-Item Env:PANTHEON_PYTHON -ErrorAction SilentlyContinue
Remove-Item Env:JARVIS_PYTHON -ErrorAction SilentlyContinue
npm.cmd test
node scripts/v2/status.js --check
git diff --check
git diff --check 718e50670812ad5da7210bd9f183521328cccf93..HEAD
git diff --name-status d4819528050b42a2dcefea590c106d912fc5e0cf..HEAD
git status --short --branch
```

Do not run browser/T3/Playwright, the local Windows launcher or lifecycle CI
test, providers/live models/commercial actions, owner-data operations, remote
Git, integration, the third gate review or P1 work.

## 8. Evidence, commits and rollback

A successful package has exactly three normal commits:

1. `A2` — this amendment, package and active state, with exact parent R2;
2. `I2` — implementation and focused tests; and
3. `S3` — completion evidence and closed state, making P0-W05 review 03 ready.

The completion record is
`docs/v2/evidence/packages/P0-W04B/COMPLETION.md`. It records exact direct,
transitive and tooling inventory, interpreter/base provenance, constraints,
commands and actual counts, limitations, preserved report blobs and rollback.
S3 may name A2 and I2; it discovers rather than self-records its own SHA.

Do not amend, squash, rebase, reset, merge, push or alter local `main`. If an
authorized check fails, preserve truthful evidence, keep P0-B04 open, do not
expand scope, and stop for the owner. Before S3, rollback is by reviewed normal
revert of I2 and A2 while preserving R1/R2 and the WIP checkpoint. After S3,
rollback remains normal reviewed revert only.

## 9. Next session

After successful S3, P0-W05 becomes ready for one completely fresh independent
review against S3. That session must write
`docs/v2/evidence/phases/P0/PHASE-GATE-REVIEW-03.md`, preserve reviews 01 and 02,
and must not integrate or start P1 without separate owner authority.

The successful package session ends exactly:

`PACKAGE COMPLETE: START A NEW SESSION FOR P0-W05-REVIEW-03`
