# Next Clean Move

Status: first corrected Agents SDK proof passed; usefulness verdict pending
Saved: 2026-07-16
Owner: Daniel
Maintainer: Codex

## Completed Foundation

- Recoverable clean baseline, encrypted source/database/artifact backups and
  authenticated restore are proven.
- A separate post-pilot encrypted checkpoint also restored successfully with
  both attempt histories, reconciled cost, source matches and 20 artifacts.
- Private GitHub baseline and clean-checkout install/start/browser proof are
  complete.
- Runtime truth, safety, commercial operating model and five-section cockpit
  are implemented.
- All 86 automated tests pass.

## Live Pilot Result

Two separately approved Demand Validator calls are preserved:

1. The original oversized contract failed structured-output parsing. It is
   reconciled as a known technical failure and remains in history.
2. The corrected lean contract succeeded with one response, 1,329 input tokens,
   335 output tokens, no tools, no handoffs and no external effects.

The corrected run passed the runtime eval at 100 and all deterministic pilot
criteria. It correctly said the fixture shows a recurring business problem but
does not prove willingness to pay. It recommended a bounded, non-paid interest
test rather than pretending that demand exists.

The provider dashboard confirms two requests, 2,742 aggregate tokens and
US$0.03 total usage. At the actual prepaid-credit conversion rate this is
A$0.05. The ledger has no remaining unknown or reserved pilot exposure.

Detailed evidence is in
`docs/reviews/DEMAND-VALIDATOR-LIVE-PILOT-2026-07-16.md`.

## Immediate Move

Daniel reviews the corrected recommendation for commercial usefulness. Suggested
starting verdict: useful, 4/5.

Suggested review note:

> Correctly separated problem evidence from proof of demand and proposed a
> bounded interest test. Future outputs should make the exact channel, sample
> size, duration and price hypothesis more concrete when evidence permits.

Do not record this verdict until Daniel confirms or changes it.

## Following Move

If Daniel marks the result useful:

1. Retain supervised status at 1/5 for reasoning over supplied evidence.
2. Prepare a distinct second fixture with its own single-use approval.
3. Connect a fresh restricted API key securely at action time; the final
   protected runtime correctly reports that no key is currently loaded.
4. Continue only to five consecutive reviewed successes; any failure resets the
   streak and creates Important Work.
5. Promote only the narrow supplied-evidence reasoning capability after an
   explicit Daniel decision.
6. Then rank three real digital-product opportunities and present one concise
   venture-selection decision.

This note is not authorization for another model call, web research, outreach,
publishing, spend, account action, legal decision or money movement.
